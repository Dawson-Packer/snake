#pragma once
#include "global.h"

class Texture {
    public:
        Texture(int ID, int x_, int y_, int w_, int h_, double r_, std::string path);

        _texture texture_data;

        



    private:


};